# Multitask

This package remains a placeholder until all four single-capability baselines work. It will contain the shared multi-head model, task router, sampler, dataloaders, and alternating-batch trainer.
